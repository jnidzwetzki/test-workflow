name: Regression Windows
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: windows-2022
    
    steps:
    - name: Test1
      shell: wsl-bash {0}
      run: |
         echo "foo=bar" >> $GITHUB_OUTPUT
         
    - name: Test2
      shell: wsl-bash {0}
      run: |
         echo $foo
         
